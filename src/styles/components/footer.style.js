import { createStyles } from '@mui/styles';

export default (theme) =>
  createStyles({
    footer: {
      backgroundColor: '#212121',
      color: '#f1f1f2',
      height: '5rem',
      display: 'flex',
      alignItems: 'center',
    },
  });
