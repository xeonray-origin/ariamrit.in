import { createStyles } from '@material-ui/styles';

export default ({ breakpoints, spacing, palette }) =>
  createStyles({
    title: {
      fontSize: '3rem',
      fontWeight: 'bold',
      fontFamily: 'Roboto',
    },
  });
